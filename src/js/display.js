import Person from './space.js'

let person = new Person();

export default class Display {
  constructor() {
    this.display = undefined;
  }

  display() {
    if ($("#inputAge" > 76)) {
      person.age()
      alert("sfsdfsd")
    }

    
    }
  }